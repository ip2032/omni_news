$(document).ready(function() {
    // ... (Your existing code)

    // New section for "Новости"
    var NEWS_SECTION_HTML = `
        <li class="nav-item nav-item-news inlb">
            <a class="nav-item-url" href="https://st.omnidesk.ru/staff/cases/list/custom/g_76066"> Новости</a>
        </li>`;

    // Append the new section to the menu
    addCode(HORIZONTAL_MENU_ELEMENTS_SELECTOR, NEWS_SECTION_HTML, true);

    // ... (Rest of your existing code)
    
    console.log("Appending Новости section to menu");
    addCode(HORIZONTAL_MENU_ELEMENTS_SELECTOR, NEWS_SECTION_HTML, true);
});
